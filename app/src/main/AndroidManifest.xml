<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gaurav.shaadisaathi">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:name=".ShaadiSaathiApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShaadiSaathi"
        android:requestLegacyExternalStorage="true">

        <!-- Phase 1 Activities -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.ShaadiSaathi.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.LoginActivity" android:exported="false" />
        <activity android:name=".activities.RegisterActivity" android:exported="false" />
        <activity android:name=".activities.HostDashboardActivity" android:exported="false" />
        <activity android:name=".activities.GuestMainActivity" android:exported="false" />

        <!-- Phase 2 Activities -->
        <activity android:name=".activities.GuestListActivity" android:exported="false" />
        <activity android:name=".activities.AddGuestActivity" android:exported="false" />
        <activity android:name=".activities.EventTimelineActivity" android:exported="false" />
        <activity android:name=".activities.AddEventActivity" android:exported="false" />
        <activity android:name=".activities.RSVPActivity" android:exported="false" />
        <activity android:name=".activities.EventDetailActivity" android:exported="false" />

        <!-- Phase 3 Activities -->
        <activity android:name=".activities.ChatListActivity" android:exported="false" />
        <activity android:name=".activities.ChatActivity" android:exported="false" />
        <activity android:name=".activities.CreateChatRoomActivity" android:exported="false" />
        <activity android:name=".activities.PhotoGalleryActivity" android:exported="false" />
        <activity android:name=".activities.PhotoViewerActivity" android:exported="false" />
        <activity android:name=".activities.CameraActivity" android:exported="false" />
        <activity android:name=".activities.PhotoUploadActivity" android:exported="false" />
        <activity android:name=".activities.NotificationActivity" android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>


        <!-- Firebase Services -->
        <service android:name=".services.FirebaseMessagingService" android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
